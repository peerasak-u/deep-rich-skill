# performance

Cost basis vs current value per holding + class totals.

```bash
python3 scripts/dr.py performance
```

Shows qty, cost ฿, value ฿, gain/loss ฿ and %. Cost from `portfolio.json`. Crypto ❓ if cost missing. Cash = 0% gain.